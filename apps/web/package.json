{
  "name": "@lotse/web",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Lotse UI-Skelett (Svelte 5 + Vite + TypeScript). Läuft im Tauri-2-Webview und als eigenständige Web-App auf Cloudflare Pages. Datenschicht ist ein Mock-Provider, wird später durch lotse-core als WebAssembly ersetzt.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "svelte": "^5.57.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tsconfig/svelte": "^5.0.4",
    "happy-dom": "^20.14.0",
    "svelte-check": "^4.7.6",
    "typescript": "^6.0.3",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
